package com.jozze.playground.topic16_performance_optimization

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for performance optimization examples. */
fun runTopic16OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic16", "Performance Optimization", "Add memory, Compose rendering, startup, baseline profile, and macrobenchmark examples.")
}
