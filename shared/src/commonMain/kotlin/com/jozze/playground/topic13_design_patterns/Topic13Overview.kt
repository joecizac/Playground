package com.jozze.playground.topic13_design_patterns

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for design pattern examples. */
fun runTopic13OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic13", "Design Patterns", "Add singleton, builder, factory, adapter, decorator, facade, observer, strategy, and command examples.")
}
