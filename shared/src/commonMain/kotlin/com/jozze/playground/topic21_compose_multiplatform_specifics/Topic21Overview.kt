package com.jozze.playground.topic21_compose_multiplatform_specifics

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for Compose Multiplatform-specific examples. */
fun runTopic21OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic21", "Compose Multiplatform Specifics", "Add shared UI, platform view, resource, navigation, and lifecycle mapping examples.")
}
