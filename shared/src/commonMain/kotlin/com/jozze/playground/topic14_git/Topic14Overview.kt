package com.jozze.playground.topic14_git

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for Git examples and references. */
fun runTopic14OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic14", "Git", "Add command references, branching, merge/rebase, conflict, cherry-pick, stash, reset, revert, and workflow examples.")
}
