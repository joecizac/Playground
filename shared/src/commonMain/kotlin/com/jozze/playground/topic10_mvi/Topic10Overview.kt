package com.jozze.playground.topic10_mvi

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for MVI examples. */
fun runTopic10OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic10", "MVI", "Add intent, state, reducer, effect/command, renderer, and MVVM comparison examples.")
}
