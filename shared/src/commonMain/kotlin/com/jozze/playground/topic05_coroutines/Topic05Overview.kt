package com.jozze.playground.topic05_coroutines

import com.jozze.playground.core.catalog.logTopicOverview
import com.jozze.playground.core.logging.LearningLogger

/** Phase 0 entry point for coroutine examples. */
fun runTopic05OverviewDemo(logger: LearningLogger) {
    logTopicOverview(logger, "topic05", "Coroutines", "Add builder, dispatcher, suspend, job, scope, structured concurrency, cancellation, exception, and channel demos.")
}
